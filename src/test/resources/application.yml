spring:
  flyway:
    enabled: false
  datasource:
    driver-class-name: org.h2.Driver
    url: jdbc:h2:mem:coretest
    username: sa
    password: sa
  jpa:
    hibernate:
      ddl-auto: create

server:
  servlet:
    context-path: /api/v1/core
